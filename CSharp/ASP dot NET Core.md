MVC and Web API merged
webforms and web pages are full .net
view pages feature for pages
only supports console apps
CLI command line interface 
ONly refernce and deploy packages you use
Kestrel is the server developed for NODE.js very fast
CLI
`dotnet new mvc` makes a set template set of files with everything you need for the mvc
`dotnet new console -o myApp` creates a simple app called myApp

Model-View-Controller

